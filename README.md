# BeHealthy (Web)

Simple React app — runs in the browser at http://localhost:3000

## Run locally

1. Start the backend (`beHealthy-main/server`) on port 3001.
2. Install and start the client:

```bash
npm install
npm start
```

The app proxies `/api` requests to the server automatically.
